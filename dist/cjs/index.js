'use strict';

var lodash = require('lodash');

// src/util.js
const add = (a, b) => a + b;

// src/index.js

const path = require('path');
console.log(lodash.merge);

console.log(path.resolve());
console.log(add(1, 2));
